{
  "type": "module",
  "name": "kitsap-jail-roster",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "postinstall": "if [ \"$NODE_ENV\" = \"production\" ]; then mkdir -p $STORAGE_DIR/frontend/dist && cp -r frontend/dist/* $STORAGE_DIR/frontend/dist; fi"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cheerio": "^1.2.0",
    "express": "^4.18.2",
    "node-cron": "^4.2.1"
  }
}